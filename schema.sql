create table
    customers (id INTEGER primary key, name TEXT, email TEXT);

insert into
    customers (name, email)
values
    ("name1", 'a@b.c');

insert into
    customers (name, email)
values
    ("name2", 'd@e.f');